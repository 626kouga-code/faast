# トレロ風タスク管理アプリ 要件定義書

## 1. 概要
Trello風のカンバンボードによるタスク管理アプリ。ブラウザ上で動作し、サーバーを持たずブラウザのlocalStorageにデータを保存する個人向けタスク管理ツール。

日々のタスクを「Todo / Doing / Done」のようなリストに分け、カードとしてタスクを書き出し、進捗に応じてドラッグで移動させながら管理することを主な利用シーンとして想定する。会員登録やインストールなしに、ブラウザを開けばすぐ使い始められる手軽さを重視する。

## 2. 目的
- 毎日のタスクを一覧化し、抜け漏れなく管理できるようにする
- タスクの状態（未着手・作業中・完了）をボード上の位置で直感的に把握できるようにする
- 期限やラベルを使って、その日・その週にやるべきことの優先度を視覚的に区別できるようにする
- ボード/リスト/カードの階層構造による柔軟なタスク整理

## 3. 対象ユーザー
- 個人で日々のタスクを管理したい利用者
- マルチユーザー・アカウント機能は持たず、単一利用者・単一ブラウザでの利用を前提とする

## 4. 非機能要件
| 項目 | 内容 |
|---|---|
| 技術スタック | React + TypeScript + Vite |
| データ永続化 | ブラウザのlocalStorage（サーバー・DB不要） |
| 対応環境 | Google Chrome / Microsoft Edge の最新2バージョン |
| 画面サイズ対応 | PC向けレイアウトのみ対応。スマートフォン等の小画面には最適化しない |
| 起動速度 | ローカル環境で初回表示3秒以内を目安とする |
| 認証 | なし（単一利用者・単一ブラウザ前提） |
| オフライン動作 | 可能（localStorageのため通信不要） |

## 5. 機能要件

### 5.1 ボード管理
- ボードを複数作成できる
- サイドバーにボード一覧を表示し、クリックで切り替えられる
- ボードのタイトルは作成時に指定し、後から名前をクリックして変更できる
- ボードを削除できる（確認ダイアログあり）

### 5.2 リスト管理
- 選択中のボード内にリスト（列）を複数作成できる
- リストのタイトルはクリックしてインライン編集できる
- リストを削除できる（配下のカードも削除される、確認ダイアログあり）
- リスト同士はドラッグ&ドロップで順序を入れ替えられる

### 5.3 カード管理
- リスト内にカードを作成できる（タイトル必須）
- カードをクリックすると詳細モーダルが開く
- カード詳細では以下を編集できる
  - タイトル
  - 説明（複数行テキスト）
  - 期限日（date input）
  - ラベル（複数選択可）
- カードを削除できる（確認ダイアログあり）
- カードはドラッグ&ドロップで同一リスト内の並び替え、および別リストへの移動ができる

### 5.4 ラベル管理
- ボードごとにラベルを作成できる（名前＋色を指定）
- カード編集画面からラベルの割り当て・解除ができる
- ラベルを削除すると、割り当てられていたカードからも自動的に外れる
- カード表面に割り当てられたラベルが色付きバッジとして表示される

### 5.5 期限管理
- カードに期限日を設定できる
- カード表面に期限が表示される
- 期限日が過去の場合、赤字で強調表示される（期限超過の視覚的通知）
- 期限日が本日の場合、専用の強調表示（例: オレンジ色や「本日締切」バッジ）を行い、当日中に対応すべきタスクを一目で判別できるようにする（未実装・今後対応）

### 5.6 検索
- ボード上部の検索バーにキーワードを入力すると、タイトル・説明にマッチするカードのみが表示される
- 検索対象は現在表示中のボードに限る

### 5.7 データ永続化
- ボード・リスト・カード・ラベルの全状態はブラウザのlocalStorageに自動保存される
- ページを再読み込みしても状態が保持される
- 初回起動時はサンプルボード（Todo/Doing/Doneの3リスト、サンプルカード、ラベル3種）が自動生成される

## 6. データモデル
```
AppState
├─ boards: Record<boardId, Board>
├─ boardOrder: boardId[]
└─ activeBoardId: string | null

Board
├─ id, title
├─ lists: Record<listId, List>
├─ listOrder: listId[]
├─ cards: Record<cardId, Card>
└─ labels: Record<labelId, Label>

List: { id, title, cardIds: cardId[] }
Card: { id, title, description?, labelIds: labelId[], dueDate? }
Label: { id, name, color }
```

## 7. 画面構成

### 7.0 レイアウト図

```
┌─────────────┬──────────────────────────────────────────────────────┐
│  サイドバー   │  ボードタイトル                          [検索バー]      │  ← 7.2 ツールバー
│             ├──────────────────────────────────────────────────────┤
│ ● マイボード  │  ┌ Todo ─────┐ ┌ Doing ────┐ ┌ Done ─────┐  [+ リスト] │
│   仕事       │  │┌─────────┐│ │┌─────────┐│ │           │            │
│   ...       │  ││[ラベル]  ││ ││[ラベル]  ││ │           │            │
│             │  ││タイトル  ││ ││タイトル  ││ │           │            │
│ [+ 新規追加] │  ││[本日締切]││ ││[期限超過]││ │           │            │  ← 7.3 カード表示
│             │  │└─────────┘│ │└─────────┘│ │           │            │
│             │  │[+カード追加]│ │            │ │           │            │
│             │  └───────────┘ └───────────┘ └───────────┘            │
└─────────────┴──────────────────────────────────────────────────────┘
        ↑ 7.1                    ↑ カードクリックで下記モーダルを表示

┌──────────────────────────────┐
│  タイトル入力欄                  │
│  説明欄（複数行）                │
│  期限日入力欄                   │
│  ラベル選択欄                   │
│                                │
│  [削除]           [キャンセル][保存]│
└──────────────────────────────┘
        ↑ 7.4 カード詳細モーダル
```

### 7.1 サイドバー（左固定）
- ボード一覧を縦に表示し、選択中のボードをハイライト表示する
- 各ボード名の右に削除ボタン（×）を配置する
- 下部に「新しいボード名」の入力欄と追加ボタンを配置する

### 7.2 ボード画面（メイン領域）
- **ツールバー**: ボードタイトル（クリックで編集可）と検索バーを左右に配置する
- **リスト一覧**: 横スクロールで複数のリストを並べる。各リストはドラッグで並び替え可能
- **リスト内**:
  - リストタイトル（クリックで編集可）と削除ボタン
  - カードを縦に並べたカードリスト（ドラッグで並び替え・リスト間移動可能）
  - 下部に「+ カードを追加」ボタン
- **リスト追加**: リスト一覧の右端に「+ リストを追加」ボタンを常設する

### 7.3 カード表示（リスト内のカード1枚）
- ラベルバッジ（複数、色付き）をカード上部に表示する
- カードタイトルを表示する
- 期限バッジを下部に表示し、状態に応じて色分けする
  - 通常（期限が先の日付）: グレー表示
  - **本日締切**: オレンジ色で強調表示し、「本日締切」であることが一目でわかるようにする（未実装・今後対応）
  - 期限超過（過去の日付）: 赤色で強調表示する

### 7.4 カード詳細モーダル
- カードクリック時にオーバーレイ表示される
- タイトル入力欄（インライン編集）
- 説明欄（複数行テキスト）
- 期限日入力欄（date input）
- ラベル選択欄（ボードのラベル一覧から複数選択、新規ラベル作成も可能）
- 下部に削除ボタンと保存/キャンセルボタンを配置する

## 8. スコープ外（今回未実装）
- 担当者（アサイン）機能
- 複数ユーザー・アカウント管理・認証
- サーバーサイド保存・複数端末間の同期
- 通知機能
- チェックリスト・添付ファイル
- カード内コメント
- ボードの並び替え

## 9. 想定される今後の拡張候補
- チェックリスト機能
- ボードの共有・複数端末同期（バックエンド導入）
- カードのアーカイブ機能
- モバイル対応レイアウト
